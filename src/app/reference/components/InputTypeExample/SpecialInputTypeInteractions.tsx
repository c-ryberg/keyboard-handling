import * as React from "react";
import { KeyboardInteractionHeader } from "../KeyboardInteractionHeader";

export const SpecialInputTypeInteractions = () => {
  return (
    <>
      <KeyboardInteractionHeader />
      <p>There are many native input types. Here are only a few examples.</p>
      <p>
        Practice nagivating through these inputs with your keyboard, such as to
        see how to change the user's selecton, or open and close a popover
        window (if present).
      </p>
    </>
  );
};
